import "tfplan"

has_valid_tags = func(resource) {
  validated = true
  print(resource)
  print(resource.applies)
  print(resource.applied.tags)
  return validated
}

main = rule {
  all tfplan.resources as _, resources {
    all resources as _, resource {
      has_valid_tags(resource)
    }
  }
}