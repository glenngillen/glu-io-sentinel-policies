import "tfplan"

main = rule {
  all tfplan.resources as _, resource {
    if resource.applied.tags is undefined {
      true
    } else {
      resource.applied.tags contains "environment"
    }
  }
}