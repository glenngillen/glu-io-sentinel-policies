import "tfplan"

main = rule {
  validated = true
  for tfplan.resources as _, resource {
    if validated {
      if resource.applied.tags is not undefined {
        validated = resource.applied.tags contains "environment"
      }
    }
  }
  validated
}